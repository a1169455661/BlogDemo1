<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.app">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />

        <!--全屏化基础Activity-->
        <activity android:name=".base.WinActivity" />

        <!--Anko框架测试-->
        <activity android:name=".module.test.AnkoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--跳转后界面-->
        <activity android:name=".module.test.SecondActivity" />


        <!--sharedPreferences封装类测试-->
        <activity android:name=".module.test.ShareActivity">

        </activity>
    </application>

</manifest>